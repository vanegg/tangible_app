// console.log("<%=@left_urls%>");
// console.log("<%=@right_urls%>");

function decodeEntities(encodedString) {
    var textArea = document.createElement('textarea');
    textArea.innerHTML = encodedString;
    return textArea.value;
}

left = JSON.parse(decodeEntities("<%=@left_urls%>"));
right = JSON.parse(decodeEntities("<%=@right_urls%>"));

console.log(left);
console.log(right);

for (var key in right){
  console.log(key + " -> " + right[key]);   
  if (key == "0"){
    $('.square_album.right .photoboard#0')
  }else if(key){

  }else if(){

  };
}